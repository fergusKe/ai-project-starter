# Engineering Prompt

先執行 `python3 workflow/bin/workflow_transition.py status`，確認 `Implementation allowed (derived): yes`。

使用 Superpowers 的工程方法進行 worktree、TDD、implementation decomposition、systematic debugging 與 review。OpenSpec 是產品需求 Source of Truth，不得建立第二套產品 Spec。

若發現需求缺漏或衝突，停止相關實作並回到 OpenSpec 修訂。
