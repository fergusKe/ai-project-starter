# Discovery → OpenSpec Prompt

根據本次 grill-with-docs 訪談、CONTEXT.md 與 ADR，建立或更新 OpenSpec change。

把所有已解決需求轉成明確、可測試的規格，包含正常流程、錯誤、權限、negative requirements、limits/defaults、security、concurrency、migration/compatibility。

完成 proposal/specs/design/tasks 後停止，不要開始 implementation，等待人工 Review。
